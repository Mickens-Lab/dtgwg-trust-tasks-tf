{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/witness/session/0.1",
  "title": "Witness Session — payload",
  "description": "Opens a witness ceremony as its own exchange, nested in a relationship exchange via parentThreadId. The response issues the session challenge; the request document's id is the value a VWC later carries as taskContext.",
  "type": "object",
  "additionalProperties": false,
  "required": ["parties"],
  "properties": {
    "parties": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "string",
        "pattern": "^did:"
      },
      "description": "The relationship DIDs of the exchange to be witnessed — the pairwise values the relationship proposal exchanged, in either order."
    },
    "ext": {
      "$ref": "../../../_framework/0.2/framework.schema.json#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Witness Session — response payload",
      "description": "The session challenge, issued under the REQUIRED proof. Presentations under witness/session/submit bind to {challenge, domain}.",
      "type": "object",
      "additionalProperties": false,
      "required": ["challenge", "domain"],
      "properties": {
        "challenge": {
          "type": "string",
          "minLength": 16,
          "description": "A fresh, unpredictable, single-use value the ceremony's presentations bind to. A witness MUST NOT reuse a challenge across sessions."
        },
        "domain": {
          "type": "string",
          "minLength": 1,
          "description": "The domain the presentations bind alongside the challenge, identifying this witness's verification context."
        },
        "ext": {
          "$ref": "../../../_framework/0.2/framework.schema.json#/$defs/Ext"
        }
      }
    }
  }
}
